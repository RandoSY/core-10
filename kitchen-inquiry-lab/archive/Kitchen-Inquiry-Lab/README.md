# Kitchen Inquiry Lab — recovered archival text

This directory preserves the surviving public-safe text edition recovered from `Kitchen-Inquiry-Lab.docx` in the user's Library.

The conversion retained the document's generated Markdown/HTML table structure and image references, but the embedded DOCX media are not yet reproduced here. The complete recovered text is split into `part-01.md` and `part-02.md` so it remains directly browsable in GitHub.

This is a historical recovered edition. The current CORE 10 architecture and later protected-experiment decisions take precedence where they supersede this document. In particular, the canonical curriculum requires the microwave-water-heating / electricity-cost sequence to remain prominent.
