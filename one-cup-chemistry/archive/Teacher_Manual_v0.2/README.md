# One Cup Chemistry — Teacher Manual v0.2

Recovered public-safe text edition from the surviving Library source `One_Cup_Chemistry_Teacher_Manual_v0.2.docx`.

The complete recovered text is split into numbered parts so it remains directly browsable and diffable in GitHub. Concatenate `part-01.md` through `part-08.md` in lexical order to reconstruct the recovered text edition.

The generated DOCX-to-Markdown text retains image/media references where they occurred in the source; those embedded media are separate forensic recovery targets.

This is a historical v0.2 edition beneath the current CORE 10 → One Cup Chemistry branch. Later canonical decisions take precedence where they supersede this edition.
